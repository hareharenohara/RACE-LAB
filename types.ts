import {createClient} from "jsr:@supabase/supabase-js@2.110.8";
import {JraProvider} from "../_shared/jra-provider.ts";
import {callGemini,MODEL} from "../_shared/gemini-client.ts";
import {PREDICTION_SCHEMA,SELECTION_SCHEMA,STRATEGIES,validatePredictions,validateSelections} from "../_shared/ai-contracts.ts";
import type {Strategy} from "../_shared/types.ts";
const json=(x:unknown,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{"content-type":"application/json"}}),unordered=new Set(["wide","quinella","trio"]);
const h=async(x:unknown)=>[...new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(JSON.stringify(x))))].map(b=>b.toString(16).padStart(2,"0")).join(""),ok=(t:string,ns:number[])=>`${t}:${(unordered.has(t)?[...ns].sort((a,b)=>a-b):ns).join("-")}`;
async function ai(db:any,batch:string,purpose:"screening"|"prediction",strategy:Strategy|null,prompt:string,schema:any){const input=await h({prompt,schema}),{data:c,error}=await db.from("ai_calls").insert({batch_run_id:batch,purpose,strategy,provider:"google",model:MODEL,prompt_version:"jra-v1",input_hash:input,request_json:{prompt,schema},status:"running"}).select("id").single();if(error)throw error;try{const r=await callGemini(prompt,schema);await db.from("ai_calls").update({status:"succeeded",response_json:r.raw,output_hash:await h(r.value),input_tokens:r.inputTokens,output_tokens:r.outputTokens,completed_at:new Date().toISOString()}).eq("id",c.id);return{...r,callId:c.id,inputHash:input}}catch(e){await db.from("ai_calls").update({status:"failed",error_message:String(e),completed_at:new Date().toISOString()}).eq("id",c.id);throw e}}
Deno.serve(async req=>{if(req.method==="GET")return json({status:"ok"});if(req.method!=="POST")return json({error:"METHOD"},405);if(req.headers.get("x-batch-secret")!==Deno.env.get("BATCH_SECRET"))return json({error:"UNAUTHORIZED"},401);const u=Deno.env.get("SUPABASE_URL"),k=Deno.env.get("SUPABASE_SECRET_KEY")??Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??(()=>{try{return JSON.parse(Deno.env.get("SUPABASE_SECRET_KEYS")??"{}").default}catch{return undefined}})();if(!u||!k)return json({error:"ENV"},500);const db=createClient(u,k,{auth:{persistSession:false}}),body=await req.json().catch(()=>({})) as {target_date?:string;force?:boolean},date=body.target_date??new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Tokyo"}).format(new Date()),weekday=new Date(`${date}T12:00:00Z`).getUTCDay();if(![0,6].includes(weekday))return json({status:"skipped"});const {data:done}=await db.from("batch_runs").select("id").eq("target_date",date).eq("parser_version","jra-netkeiba-v1").eq("status","succeeded").maybeSingle();if(done&&!body.force)return json({status:"already_completed",batchRunId:done.id});const {data:batch,error:be}=await db.from("batch_runs").insert({target_date:date,parser_version:"jra-netkeiba-v1"}).select("id").single();if(be)return json({error:be.message},500);try{const provider=new JraProvider(),races=await provider.getRaceList(date);if(!races.length)throw new Error("NO_JRA_RACES");const rows=races.map(r=>({external_id:r.externalId,race_date:r.raceDate,track:r.track,race_number:r.raceNumber,race_name:r.raceName,start_time:r.startTime,surface:r.surface,distance:r.distance,source_url:r.sourceUrl,source_fetched_at:new Date().toISOString(),source_hash:"jra-list-v1"})),{data:saved,error:re}=await db.from("races").upsert(rows,{onConflict:"external_id"}).select("id,external_id,track,race_number,race_name,start_time,surface,distance");if(re)throw re;const screenInput=(saved??[]).map(r=>({race_id:r.id,track:r.track,race_number:r.race_number,race_name:r.race_name,start_time:r.start_time,surface:r.surface,distance:r.distance})),screen=await ai(db,batch.id,"screening",null,`JRA中央競馬の一覧から保守型・バランス型・積極型ごとに最大3レースを選定。無理に選ばず空配列可。race_id厳守。\n${JSON.stringify(screenInput)}`,SELECTION_SCHEMA),valid=new Set(screenInput.map(r=>String(r.race_id))),sel=validateSelections(screen.value,valid);for(const s of STRATEGIES)for(let i=0;i<sel[s].length;i++)await db.from("race_selections").insert({batch_run_id:batch.id,race_id:sel[s][i].race_id,strategy:s,score:sel[s][i].score,reason:sel[s][i].reason,rank:i+1,ai_call_id:screen.callId});const chosen=new Set(STRATEGIES.flatMap(s=>sel[s].map(x=>x.race_id))),details=new Map<string,Awaited<ReturnType<JraProvider["getDetail"]>>>();for(const id of chosen){const row=(saved??[]).find(r=>r.id===id)!,summary=races.find(r=>r.externalId===row.external_id)!;const d=await provider.getDetail(summary);details.set(id,d);for(const e of d.entries){const horseId=String(e.sourceData.horse_id),{data:horse,error:he}=await db.from("horses").upsert({external_id:`jra:${horseId}`,name:e.horseName,sex:e.sex,birth_year:e.age?Number(date.slice(0,4))-e.age:null},{onConflict:"external_id"}).select("id").single();if(he)throw he;const win=d.odds.find(o=>o.type==="win"&&o.horses[0]===e.horseNumber),place=d.odds.find(o=>o.type==="place"&&o.horses[0]===e.horseNumber);await db.from("race_entries").upsert({race_id:id,horse_id:horse.id,horse_number:e.horseNumber,gate_number:e.gateNumber,jockey:e.jockey,trainer:e.trainer,weight_carried:e.weightCarried,horse_weight:e.horseWeight,horse_weight_delta:e.horseWeightDelta,win_odds:win?.odds,place_odds_low:place?.odds,place_odds_high:place?.oddsMax,popularity:win?.popularity,raw_data:{provider:"netkeiba"}},{onConflict:"race_id,horse_number"})}}let pc=0,bc=0;for(const s of STRATEGIES){const ids=new Set(sel[s].map(x=>x.race_id)),input=[...ids].map(id=>{const d=details.get(id)!;return{race_id:id,race:d.race,entries:d.entries,odds:d.odds.sort((a,b)=>(a.popularity??9999)-(b.popularity??9999)).slice(0,350)}}),{data:account}=await db.from("strategy_accounts").select("current_balance").eq("strategy",s).single(),pr=await ai(db,batch.id,"prediction",s,`JRAの${s}戦略。残高${account?.current_balance??100000}円。公式相当オッズと推定確率から期待値を判断し、入力にある買い目だけ100円単位で提案。弱ければSKIP。\n${JSON.stringify(input)}`,PREDICTION_SCHEMA),numbers=new Map(input.map(x=>[String(x.race_id),new Set(x.entries.map(e=>e.horseNumber))])),checked=validatePredictions(pr.value,s,ids,numbers),market=new Map<string,number>();for(const x of input)for(const o of x.odds)market.set(`${x.race_id}:${ok(o.type,o.horses)}`,o.odds);for(const p of checked.predictions){const {data:pred,error:pe}=await db.from("predictions").insert({race_id:p.race_id,strategy:s,ai_call_id:pr.callId,action:p.action==="BET"?"bet":"skip",confidence:p.confidence,reason:p.reason,input_hash:pr.inputHash,prediction_hash:await h(p),predicted_at:new Date().toISOString(),raw_response:p}).select("id").single();if(pe)throw pe;pc++;for(const b of p.bets as any[]){const odds=market.get(`${p.race_id}:${ok(b.type,b.horses)}`);if(!odds)throw new Error("ODDS_NOT_FOUND");await db.from("bets").insert({prediction_id:pred.id,race_id:p.race_id,strategy:s,bet_type:b.type,combination:b.horses,stake:b.stake,odds_at_prediction:odds,estimated_probability:b.estimated_probability,expected_value:odds*b.estimated_probability});bc++}}}await db.from("batch_runs").update({status:"succeeded",races_fetched:races.length,api_requests:2+chosen.size*2+4,finished_at:new Date().toISOString(),metadata:{provider:"jra_netkeiba",details:chosen.size,predictions:pc,bets:bc}}).eq("id",batch.id);return json({status:"succeeded",races:races.length,details:chosen.size,predictions:pc,bets:bc,batchRunId:batch.id})}catch(e){await db.from("batch_runs").update({status:"failed",error_message:e instanceof Error?e.message:String(e),finished_at:new Date().toISOString()}).eq("id",batch.id);return json({error:"FAILED",detail:e instanceof Error?e.message:String(e)},500)}});
